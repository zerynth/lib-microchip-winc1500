# configuration for winc1500
#
#

config:
    # enable sockets
    ZERYNTH_SOCKETS: true
    ZERYNTH_EXTERNAL_LWIP: true 
options:
    ZERYNTH_SSL:
        help: >
            If enabled, the support for TLS/SSL socket will be available in the driver
